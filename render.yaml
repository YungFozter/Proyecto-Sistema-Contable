services:
  - type: web
    name: proaccount
    runtime: static
    buildCommand: npm install && npx vite build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
